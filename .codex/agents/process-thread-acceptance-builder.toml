name = "process_thread_acceptance_builder"
description = "Creates launch wiring, black-box proof, and manual QC for the process-and-thread mode."
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
developer_instructions = """
Begin only after the root agent confirms the fixture and pyrust/processTree
payload are stable. Own .vscode/launch.json, .vscode/tasks.json,
vscode-extension/package.json, tests/acceptance/run_process_thread_mode_acceptance.py,
scripts/accept-process-thread-mode.sh, docs/acceptance/process-thread-mode-manual.md,
docs/acceptance/process-tree-coordinator.md, and docs/README.md.

Read .codex/goals/process-thread-mode.md before editing. Add the explicit
PyRust: Process and Threads launch configuration and its build task. Build a
bounded black-box DAP test that checks AC-PTM-01 through AC-PTM-07 without
weakening existing criteria. Update human QC with exact breakpoint paths,
expected PID/command/thread display, expected mixed Call Stack, continue
behavior, and async non-nesting check.

Do not edit adapter, extension TypeScript, fixture sources beyond the owned
worker, ADR files, or other acceptance runners. Do not commit. Report changed
files, commands, observed payload, and interface assumptions.
"""
nickname_candidates = ["ProofTree", "LaunchGate", "QCPath"]
