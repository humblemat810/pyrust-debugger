[project]
name = "pyrust-debugger"
version = "0.0.1"
requires-python = ">=3.14,<3.15"
license = { text = "Apache-2.0" }

[tool.uv]
package = false

[dependency-groups]
dev = [
    "debugpy==1.8.20",
    "maturin==1.14.1",
]
