[package]
name = "pyrust-subinterp"
version = "0.1.0"
edition = "2024"

[lib]
name = "pyrust_subinterp"
crate-type = ["cdylib"]

[dependencies]
pyo3-ffi = { version = "0.29.0", features = ["extension-module"] }

[build-dependencies]
pyo3-build-config = "0.29.0"
