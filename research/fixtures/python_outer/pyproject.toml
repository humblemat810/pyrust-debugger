[build-system]
requires = ["maturin>=1.14,<2"]
build-backend = "maturin"

[project]
name = "pyrust-native"
version = "0.1.0"
requires-python = ">=3.14,<3.15"

[tool.maturin]
manifest-path = "Cargo.toml"
